export function convertDraftToHtml(draftJson: any): string {
    // This could just return the draft JSON as a string, or do a real conversion.
    // For now, let’s assume the frontend sends HTML directly. We'll store as is.
    return draftJson;
}